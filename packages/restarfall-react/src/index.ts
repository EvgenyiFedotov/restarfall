import { useCall, useChange, useEvent, useStore } from "./hooks";

const use = {
  call: useCall,
  change: useChange,
  event: useEvent,
  store: useStore,
};

export { use };
export { Provider, useCall, useChange, useEvent, useStore } from "./hooks";
